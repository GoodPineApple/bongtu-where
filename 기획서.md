# Project: "봉투어디" (Bongtu-Where)

> 종량제 봉투 재고 불안감 해소를 위한 실시간 재고 확인 웹 서비스 (MVP)

## 1. Context & Objective

- 최근 종량제 봉투 수급 불안으로 인한 사용자들의 구매 어려움을 해결함.
- 공공데이터포털의 판매소 API를 활용하여 내 주변 판매점 위치를 지도에 시각화함.
- 서버/DB 구축 없이 프론트엔드 UI/UX와 클라이언트 사이드 상태 관리에 집중함.

## 2. Tech Stack

- **Framework:** React (Vite)
- **Styling:** Tailwind CSS
- **Icons:** Lucide React
- **Map Library:** Kakao Maps SDK (또는 Naver Maps)
- **State Management:** React 단방향 props 사용 (Redux나 Zustand와 같은 state관리도구 사용 금지)

## 3. Data Schema (Based on Public Data Portal)

공공데이터포털의 '전국종량제봉투판매소표준데이터' 응답 형식을 기반으로 인터페이스를 설계한다.

| Field Name    | Type     | Description                | Mapping (Original)                    |
| :------------ | :------- | :------------------------- | :------------------------------------ |
| `id`          | string   | 고유 식별자                | -                                     |
| `storeName`   | string   | 판매소 명칭                | `판매소명`                            |
| `address`     | string   | 도로명 주소                | `소재지도로명주소`                    |
| `lat`         | number   | 위도                       | `위도`                                |
| `lng`         | number   | 경도                       | `경도`                                |
| `phone`       | string   | 전화번호                   | `전화번호`                            |
| `bagTypes`    | string[] | 취급 봉투 종류             | `판매대상종량제봉투종류` (Split 필요) |
| `stockStatus` | string   | 재고 상태 (EMPTY/FEW/FULL) | UI Mock Data                          |
| `updatedAt`   | string   | 마지막 업데이트 시각       | UI Mock Data                          |

## 4. UI/UX Requirements

### 4.1. Main Layout (Map-Centric)

- **Full Screen Map:** 사용자의 현재 위치(Geolocation)를 기반으로 지도를 초기화한다.
- **Custom Markers:** `stockStatus`에 따라 마커 색상을 다르게 렌더링한다.
  - **Green (FULL):** 재고 있음 (10개 이상)
  - **Yellow (FEW):** 품절 임박 (5개 미만)
  - **Red (EMPTY):** 품절
- **Floating UI:**
  - **Top:** 지역명 검색창 및 규격 필터(5L, 10L, 20L 등).
  - **Bottom Right:** 현재 위치로 이동 버튼.

### 4.2. Bottom Sheet (Store Detail)

- 마커 클릭 시 하단에서 슬라이딩 애니메이션과 함께 등장.
- **Contents:**
  - 판매점 정보: 이름, 거리(현재 위치 기준 계산), 주소, 전화번호(클릭 시 연결).
  - 취급 항목: 해당 지점에서 취급하는 봉투 리스트를 태그 형태로 표시.
  - **User Report (Simulation):** "재고 있음", "소량 남음", "품절" 버튼 3개 배치. 버튼 클릭 시 해당 마커의 상태가 UI상에서 즉시 변경되는 인터랙션 포함.

## 5. Implementation Steps (Instructions for AI)

1. **Setup:** Tailwind CSS와 지도 SDK를 설정하고 기본 레이아웃을 구성하라.
2. **Data Integration:** 공공데이터 API 응답 형식을 모사한 Mock Data(최소 10개 지점)를 생성하라. 이때 좌표값은 서울 혹은 특정 테스트 지역으로 설정하라.
3. **Map Logic:**
   - 지도가 로드되면 Mock Data를 순회하며 커스텀 마커를 생성하라.
   - 마커 클릭 시 `selectedStore` 상태를 업데이트하고 Bottom Sheet를 활성화하라.
4. **Interactions:**
   - 상단 필터(예: 20L) 선택 시, 해당 규격을 취급하지 않는 판매점 마커는 지도에서 숨김 처리하라.
   - 제보 버튼 클릭 시 `toast` 메시지와 함께 마커 색상이 변경되는 시뮬레이션을 구현하라.

## 6. Design Concept

- **Color Palette:** Primary (#3B82F6, 신뢰감을 주는 블루), Success (#22C55E), Warning (#F59E0B), Danger (#EF4444).
- **Typography:** 가독성이 좋은 Sans-serif 계열 (Pretendard 등).
- **Feel:** 당근마켓이나 배달의민족처럼 친근하면서도 정보 전달이 명확한 모바일 웹 스타일.
