AI 에이전트가 즉시 코드를 작성하고 아키텍처를 설계할 수 있도록, **상단 네비게이션**과 **익명 커뮤니티** 기능이 포함된 업데이트된 기획문서(requirements.md)입니다.

---

````markdown
# Project: "봉투어디" (Bongtu-Where) - Full Version

> 지도 기반 재고 확인 및 익명 커뮤니티 통합 서비스

## 1. Project Overview

- **핵심 가치:** 실시간 재고 정보(지도)와 사용자 경험 공유(커뮤니티)를 결합하여 종량제 봉투 수급 불안을 해소함.
- **주요 업데이트:**
  - 탭 기반 네비게이션 (지도 <-> 커뮤니티)
  - 익명 게시판 및 댓글 시스템

## 2. Tech Stack

- **Frontend:** React (Vite), Tailwind CSS
- **Icons:** Lucide React
- **State Management:** React `useState`, `useMemo` (탭 전환 및 게시글 관리)
- **Animation:** Framer Motion (탭 전환 및 바텀시트 애니메이션 권장)

## 3. Component Architecture

### 3.1. Global Navigation Bar (Top)

- **Sticky Header:** 화면 상단 고정.
- **Logo:** 좌측 배치 ("봉투어디").
- **Tabs:** 중앙 또는 우측 배치.
  - [지도]: 클릭 시 지도 뷰로 전환.
  - [커뮤니티]: 클릭 시 게시판 뷰로 전환.
- **Active State:** 현재 머무르는 탭에 강조색(Primary Blue) 및 언더라인 표시.

### 3.2. View 1: 봉투어디 지도 (Map View)

- _기존 기획 유지_
- 공공데이터 기반 판매점 마커 표시.
- 재고 상태(신호등 색상) 표시.
- 하단 판매점 상세 정보 바텀시트.

### 3.3. View 2: 봉투어디 커뮤니티 (Community View)

- **익명 게시판 구조:** 로그인 없이 글쓰기/댓글 가능 (익명 닉네임 자동 생성 로직 포함).
- **Post List:**
  - 카드형 레이아웃 (제목, 본문 일부, 작성 시간, 댓글 수, 추천 수).
  - 카테고리 태그 (예: [정보공유], [질문], [잡담]).
- **Post Detail:**
  - 게시글 전문 및 작성자(익명).
  - 댓글 리스트 및 댓글 작성창.
- **Write Post:**
  - 글쓰기 버튼 (Floating Action Button).
  - 제목, 본문, 카테고리 선택 폼.

## 4. Data Schema

### 4.1. Community Post

```typescript
interface Post {
  id: string;
  category: "INFO" | "QUESTION" | "CHAT";
  title: string;
  content: string;
  author: string; // 예: "익명의 20리터", "동네 보안관"
  createdAt: string;
  likes: number;
  comments: Comment[];
}
```
````

### 4.2. Comment

```typescript
interface Comment {
  id: string;
  postId: string;
  content: string;
  author: string;
  createdAt: string;
}
```

## 5. UI/UX Requirements

- **View Switching:** 탭 클릭 시 페이지 새로고침 없이 부드럽게 뷰가 교체되어야 함 (Conditional Rendering).
- **Anonymous Identity:**
- 글 작성 시 무작위 형용사 + 봉투 관련 단어 조합으로 닉네임 자동 생성 (예: "배고픈 10리터", "빠른 재사용").

- **Community Interaction:**
- 게시글 리스트에서 특정 글 클릭 시 상세 페이지로 진입 (또는 Modal/Overlay).
- '좋아요' 버튼 클릭 시 카운트 증가 시뮬레이션.

## 6. Implementation Steps (Instructions for AI)

1. **Global Layout:** `App.jsx`에서 상단 네비게이션 바를 구현하고, `activeTab` 상태에 따라 `MapView`와 `CommunityView` 컴포넌트를 스위칭하라.
2. **Community View:**

- Mock Data를 사용하여 게시글 리스트를 구현하라.
- 게시글 작성 폼을 만들고, 작성 완료 시 리스트 상단에 추가되는 로직을 작성하라 (Client-side state).

3. **Map-Community Link:**

- 게시글 본문에 특정 판매점 이름이 언급되면 지도 뷰로 이동하는 링크 버튼(Optional)을 고려하라.

4. **Design:**

- 커뮤니티는 가독성을 위해 흰색 배경에 깔끔한 보더를 가진 카드 스타일을 사용하라.
- 네비게이션 바는 `backdrop-blur` 효과를 주어 세련된 느낌을 구현하라.

## 7. Mock Data (Community Example)

- "오늘 OO동 마트 20L 재고 넉넉하네요!" (카테고리: 정보공유)
- "75L 봉투 파는 곳 보통 어디에 있나요?" (카테고리: 질문)
- "마스크 대란 때 생각나서 무섭네요 ㅠㅠ" (카테고리: 잡담)

```

이 문서를 AI 도구에 입력하면, **상단 네비게이션을 통해 두 기능을 자유롭게 오가는 싱글 페이지 애플리케이션(SPA)** 형태의 프로토타입을 제작해 줄 것입니다.

```
